'use client'

import { useMemo, useState } from 'react'
import {
  Activity,
  AlertCircle,
  ArrowDownRight,
  ArrowUpRight,
  BarChart3,
  Bell,
  CalendarDays,
  Check,
  CheckCircle2,
  ChevronDown,
  ClipboardCheck,
  Clock3,
  FileCheck2,
  FileText,
  FolderKanban,
  LayoutDashboard,
  Menu,
  PanelLeftClose,
  Plus,
  Search,
  Settings,
  ShieldCheck,
  SlidersHorizontal,
  Users,
  X,
} from 'lucide-react'
import { Avatar, AvatarFallback } from '@/components/ui/avatar'
import { Badge } from '@/components/ui/badge'
import { Button } from '@/components/ui/button'
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'
import { Input } from '@/components/ui/input'
import { Progress } from '@/components/ui/progress'
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'
import { Separator } from '@/components/ui/separator'
import { Textarea } from '@/components/ui/textarea'

const reports = [
  { range: 'May 20 – May 26, 2024', project: 'Product Design', status: 'Needs Correction' },
  { range: 'May 13 – May 19, 2024', project: 'Product Design', status: 'Submitted' },
  { range: 'May 06 – May 12, 2024', project: 'Website Redesign', status: 'Approved' },
  { range: 'Apr 29 – May 05, 2024', project: 'Website Redesign', status: 'Approved' },
  { range: 'Apr 22 – Apr 28, 2024', project: 'Product Design', status: 'Draft' },
  { range: 'Apr 15 – Apr 21, 2024', project: 'Internal Operations', status: 'Submitted' },
]

const tasks = [
  { name: 'Finalize onboarding flow', priority: 'High', planned: 92, actual: 84, status: 'Completed', time: '6h 30m', output: 'Prototype v3' },
  { name: 'Review analytics dashboard', priority: 'Medium', planned: 78, actual: 72, status: 'In progress', time: '4h 15m', output: 'Insights doc' },
  { name: 'Update help center content', priority: 'Low', planned: 64, actual: 64, status: 'Completed', time: '3h 45m', output: '12 articles' },
  { name: 'Stakeholder sync and notes', priority: 'Medium', planned: 100, actual: 90, status: 'Completed', time: '2h 00m', output: 'Decision log' },
]

const activity = [
  { icon: FileCheck2, title: 'Weekly report submitted', detail: 'May 20 – May 26 · Product Design', time: '2 hours ago', tone: 'primary' },
  { icon: AlertCircle, title: 'Report needs correction', detail: 'May 13 – May 19 · Product Design', time: 'Yesterday', tone: 'accent' },
  { icon: CheckCircle2, title: 'Report approved', detail: 'May 06 – May 12 · Website Redesign', time: '3 days ago', tone: 'success' },
  { icon: Clock3, title: 'Draft saved', detail: 'May 20 – May 26 · Internal Operations', time: 'May 24', tone: 'muted' },
]

const teamMembers = [
  { initials: 'JM', name: 'Jamie Morgan', role: 'Product Designer', reports: '12 / 12', compliance: 100, status: 'On track' },
  { initials: 'AL', name: 'Alex Lee', role: 'Frontend Engineer', reports: '10 / 12', compliance: 83, status: 'At risk' },
  { initials: 'SK', name: 'Sam Kim', role: 'Product Manager', reports: '12 / 12', compliance: 100, status: 'On track' },
  { initials: 'NR', name: 'Nora Reed', role: 'UX Researcher', reports: '11 / 12', compliance: 92, status: 'On track' },
]

const projects = [
  { name: 'Product Design', code: 'PD-042', members: 8, completion: 78, reports: '32 this month', color: 'bg-primary' },
  { name: 'Website Redesign', code: 'WEB-118', members: 5, completion: 64, reports: '21 this month', color: 'bg-chart-2' },
  { name: 'Internal Operations', code: 'OPS-009', members: 12, completion: 91, reports: '44 this month', color: 'bg-accent' },
]

type View = 'Overview' | 'Reports' | 'Team' | 'Projects' | 'Reviews' | 'Settings'

function Sidebar({ view, onNavigate }: { view: View; onNavigate: (view: View) => void }) {
  const links = [
    { label: 'Overview' as View, icon: LayoutDashboard },
    { label: 'Reports' as View, icon: FileText },
    { label: 'Team' as View, icon: Users },
    { label: 'Projects' as View, icon: FolderKanban },
  ]
  return <aside className="hidden w-64 shrink-0 flex-col border-r border-sidebar-border bg-sidebar lg:flex">
    <div className="flex h-20 items-center gap-3 px-6"><div className="flex size-9 items-center justify-center rounded-xl bg-sidebar-primary text-sidebar-primary-foreground"><ClipboardCheck className="size-5" aria-hidden="true" /></div><div><p className="font-semibold tracking-tight text-sidebar-foreground">Pulseboard</p><p className="text-xs text-sidebar-foreground/60">Weekly reporting</p></div></div>
    <Separator />
    <nav className="flex flex-1 flex-col gap-1 px-3 py-6" aria-label="Primary navigation"><p className="px-3 pb-2 text-[11px] font-semibold uppercase tracking-[0.16em] text-sidebar-foreground/45">Workspace</p>{links.map(({ label, icon: Icon }) => <button key={label} onClick={() => onNavigate(label)} className={`flex items-center gap-3 rounded-lg px-3 py-2.5 text-sm transition-colors ${view === label ? 'bg-sidebar-accent font-medium text-sidebar-accent-foreground' : 'text-sidebar-foreground/65 hover:bg-sidebar-accent/70 hover:text-sidebar-foreground'}`}><Icon className="size-4" aria-hidden="true" />{label}</button>)}<div className="mt-7"><p className="px-3 pb-2 text-[11px] font-semibold uppercase tracking-[0.16em] text-sidebar-foreground/45">Manage</p><button onClick={() => onNavigate('Reviews')} className={`flex w-full items-center gap-3 rounded-lg px-3 py-2.5 text-sm ${view === 'Reviews' ? 'bg-sidebar-accent font-medium text-sidebar-accent-foreground' : 'text-sidebar-foreground/65 hover:bg-sidebar-accent/70 hover:text-sidebar-foreground'}`}><ShieldCheck className="size-4" />Reviews</button><button onClick={() => onNavigate('Settings')} className={`flex w-full items-center gap-3 rounded-lg px-3 py-2.5 text-sm ${view === 'Settings' ? 'bg-sidebar-accent font-medium text-sidebar-accent-foreground' : 'text-sidebar-foreground/65 hover:bg-sidebar-accent/70 hover:text-sidebar-foreground'}`}><Settings className="size-4" />Settings</button></div></nav>
    <div className="border-t border-sidebar-border p-4"><div className="flex items-center gap-3 rounded-xl bg-sidebar-accent/70 p-3"><Avatar className="size-9"><AvatarFallback className="bg-sidebar-primary text-xs text-sidebar-primary-foreground">JM</AvatarFallback></Avatar><div className="min-w-0 flex-1"><p className="truncate text-sm font-medium text-sidebar-foreground">Jamie Morgan</p><p className="text-xs text-sidebar-foreground/60">Team Member</p></div><ChevronDown className="size-4 text-sidebar-foreground/50" /></div></div>
  </aside>
}

function StatusBadge({ status }: { status: string }) {
  const styles: Record<string, string> = { Draft: 'border-muted-foreground/25 bg-muted text-muted-foreground', Submitted: 'border-primary/25 bg-primary/10 text-primary', 'Needs Correction': 'border-accent/40 bg-accent/15 text-accent-foreground', Approved: 'border-chart-2/30 bg-chart-2/10 text-chart-2', 'On track': 'border-chart-2/30 bg-chart-2/10 text-chart-2', 'At risk': 'border-accent/40 bg-accent/15 text-accent-foreground', Completed: 'border-chart-2/30 bg-chart-2/10 text-chart-2', 'In progress': 'border-primary/25 bg-primary/10 text-primary' }
  return <Badge variant="outline" className={styles[status] ?? 'border-muted-foreground/25 bg-muted text-muted-foreground'}>{status}</Badge>
}

function PageHeading({ eyebrow, title, description, action }: { eyebrow: string; title: string; description: string; action?: React.ReactNode }) {
  return <section className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div className="flex flex-col gap-1"><p className="text-xs font-medium uppercase tracking-[0.14em] text-muted-foreground">{eyebrow}</p><h2 className="text-balance text-2xl font-semibold tracking-tight sm:text-3xl">{title}</h2><p className="text-pretty text-sm leading-6 text-muted-foreground">{description}</p></div>{action}</section>
}

function OverviewView({ onNavigate }: { onNavigate: (view: View) => void }) {
  const metrics = [
    { label: 'Reports this week', value: '42', change: '+8.4%', direction: 'up', icon: FileText },
    { label: 'Compliance rate', value: '94.2%', change: '+2.1%', direction: 'up', icon: CheckCircle2 },
    { label: 'Needs correction', value: '6', change: '-12.5%', direction: 'down', icon: AlertCircle },
    { label: 'Open blockers', value: '9', change: '+3', direction: 'up', icon: Activity },
  ]
  return <div className="flex flex-col gap-6"><PageHeading eyebrow="Team member workspace" title="Good morning, Jamie" description="Here is how your team is tracking this week." action={<Button onClick={() => onNavigate('Reports')}><Plus data-icon="inline-start" />New weekly report</Button>} />
    <div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{metrics.map(({ label, value, change, direction, icon: Icon }) => <Card key={label} className="shadow-none"><CardContent className="flex items-start justify-between p-5"><div className="flex flex-col gap-3"><p className="text-sm text-muted-foreground">{label}</p><p className="text-3xl font-semibold tracking-tight">{value}</p><div className="flex items-center gap-1 text-xs text-muted-foreground"><span className={direction === 'down' ? 'text-chart-2' : 'text-primary'}>{direction === 'down' ? <ArrowDownRight className="inline size-3" /> : <ArrowUpRight className="inline size-3" />}{change}</span> vs last week</div></div><div className="rounded-lg bg-muted p-2.5 text-muted-foreground"><Icon className="size-5" /></div></CardContent></Card>)}</div>
    <div className="grid gap-6 xl:grid-cols-[1.45fr_1fr]"><Card className="shadow-none"><CardHeader><CardTitle className="text-base">Tasks completed trend</CardTitle><CardDescription>Across your team · Last 6 weeks</CardDescription></CardHeader><CardContent><div className="flex h-56 items-end gap-3 border-b border-l px-3 pb-0 pt-8 sm:gap-6">{[44, 59, 51, 73, 68, 86].map((height, index) => <div key={index} className="flex h-full flex-1 flex-col justify-end gap-2"><div className="rounded-t-md bg-primary/80 transition-all hover:bg-primary" style={{ height: `${height}%` }} /><span className="text-center text-[11px] text-muted-foreground">W{index + 1}</span></div>)}</div></CardContent></Card><Card className="shadow-none"><CardHeader><CardTitle className="text-base">Workload by project</CardTitle><CardDescription>Current team allocation</CardDescription></CardHeader><CardContent><div className="flex items-center gap-6"><div className="relative flex size-36 shrink-0 items-center justify-center rounded-full" style={{ background: 'conic-gradient(var(--primary) 0 46%, var(--chart-2) 46% 76%, var(--accent) 76% 100%)' }}><div className="flex size-20 items-center justify-center rounded-full bg-card text-center"><span className="text-xs text-muted-foreground">Total<br /><strong className="text-lg text-foreground">100%</strong></span></div></div><div className="flex flex-col gap-3 text-sm">{[['Product Design', '46%', 'bg-primary'], ['Website Redesign', '30%', 'bg-chart-2'], ['Operations', '24%', 'bg-accent']].map(([name, percentage, color]) => <div key={name} className="flex items-center gap-2"><span className={`size-2 rounded-full ${color}`} /><span className="text-muted-foreground">{name}</span><span className="ml-auto font-medium">{percentage}</span></div>)}</div></div></CardContent></Card></div>
    <Card className="shadow-none"><CardHeader className="flex-row items-center justify-between"><div><CardTitle className="text-base">Recent activity</CardTitle><CardDescription>Latest submissions and review actions</CardDescription></div><Button variant="ghost" size="sm" onClick={() => onNavigate('Reports')}>View all</Button></CardHeader><CardContent className="flex flex-col gap-1">{activity.map(({ icon: Icon, title, detail, time, tone }) => <div key={title} className="flex items-start gap-3 rounded-lg p-3 hover:bg-muted/50"><div className={`mt-0.5 rounded-full p-2 ${tone === 'primary' ? 'bg-primary/10 text-primary' : tone === 'accent' ? 'bg-accent/15 text-accent-foreground' : tone === 'success' ? 'bg-chart-2/10 text-chart-2' : 'bg-muted text-muted-foreground'}`}><Icon className="size-4" /></div><div className="min-w-0 flex-1"><p className="text-sm font-medium">{title}</p><p className="truncate text-xs text-muted-foreground">{detail}</p></div><time className="shrink-0 text-xs text-muted-foreground">{time}</time></div>)}</CardContent></Card>
  </div>
}

function ReportsView({ onNavigate }: { onNavigate: (view: View) => void }) {
  const [tab, setTab] = useState<'history' | 'new'>('history')
  const [query, setQuery] = useState('')
  const [status, setStatus] = useState('All statuses')
  const [submitted, setSubmitted] = useState(false)
  const filtered = useMemo(() => reports.filter((report) => (status === 'All statuses' || report.status === status) && `${report.range} ${report.project} ${report.status}`.toLowerCase().includes(query.toLowerCase())), [query, status])
  if (tab === 'new') return <ReportForm onBack={() => setTab('history')} onSubmitted={() => setSubmitted(true)} submitted={submitted} />
  return <div className="flex flex-col gap-6"><PageHeading eyebrow="Team member workspace" title="Your reports" description="Review past reports or create a new weekly update." action={<Button onClick={() => { setSubmitted(false); setTab('new') }}><Plus data-icon="inline-start" />New report</Button>} /><div className="flex gap-1 border-b"><button onClick={() => setTab('history')} className="border-b-2 border-primary px-3 py-2 text-sm font-medium text-primary">Report history</button><button onClick={() => onNavigate('Reviews')} className="px-3 py-2 text-sm text-muted-foreground">Review feedback</button></div><Card className="shadow-none"><CardContent className="flex flex-col gap-4 p-4 sm:flex-row sm:items-end"><div className="flex min-w-0 flex-1 flex-col gap-1.5"><label htmlFor="search-reports" className="text-xs font-medium text-muted-foreground">Search reports</label><div className="relative"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground" aria-hidden="true" /><Input id="search-reports" value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search by project or date" className="pl-9" /></div></div><div className="flex min-w-0 flex-col gap-1.5 sm:w-52"><label className="text-xs font-medium text-muted-foreground">Filter by status</label><Select value={status} onValueChange={setStatus}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="All statuses">All statuses</SelectItem><SelectItem value="Draft">Draft</SelectItem><SelectItem value="Submitted">Submitted</SelectItem><SelectItem value="Needs Correction">Needs Correction</SelectItem><SelectItem value="Approved">Approved</SelectItem></SelectContent></Select></div></CardContent></Card><Card className="shadow-none"><CardHeader><CardTitle className="text-base">Report history</CardTitle><CardDescription>{filtered.length} {filtered.length === 1 ? 'report' : 'reports'} found</CardDescription></CardHeader><CardContent className="p-0"><div className="overflow-x-auto"><table className="w-full min-w-[680px] text-sm"><thead><tr className="border-y bg-muted/40 text-left text-xs font-medium text-muted-foreground"><th className="px-6 py-3">Week date range</th><th className="px-4 py-3">Project tag</th><th className="px-4 py-3">Status</th><th className="px-6 py-3 text-right">Actions</th></tr></thead><tbody>{filtered.map((report) => <tr key={`${report.range}-${report.project}`} className="border-b last:border-0"><td className="px-6 py-4"><div className="flex items-center gap-2 font-medium"><CalendarDays className="size-4 text-muted-foreground" aria-hidden="true" />{report.range}</div></td><td className="px-4 py-4"><Badge variant="secondary">{report.project}</Badge></td><td className="px-4 py-4"><StatusBadge status={report.status} /></td><td className="px-6 py-4 text-right"><Button variant="outline" size="sm" onClick={() => setTab('new')}>View/Edit</Button></td></tr>)}{filtered.length === 0 && <tr><td colSpan={4} className="px-6 py-12 text-center text-muted-foreground">No reports match your filters.</td></tr>}</tbody></table></div></CardContent></Card></div>
}

function ReportForm({ onBack, onSubmitted, submitted }: { onBack: () => void; onSubmitted: () => void; submitted: boolean }) {
  const [reportStatus, setReportStatus] = useState('Needs Correction')
  return <div className="flex flex-col gap-6"><PageHeading eyebrow="Weekly report" title="May 20 – May 26, 2024" description="Capture the work you completed and share what is next." action={<Button variant="outline" onClick={onBack}>Back to reports</Button>} />{reportStatus === 'Needs Correction' && <div className="flex items-start gap-3 rounded-lg border border-accent/30 bg-accent/10 p-4 text-sm"><AlertCircle className="mt-0.5 size-5 shrink-0 text-accent-foreground" /><div><p className="font-medium text-accent-foreground">Manager feedback</p><p className="mt-1 leading-6 text-accent-foreground/80">Please add more detail to the output for the onboarding flow and clarify the time spent.</p></div></div>}<Card className="shadow-none"><CardHeader className="flex-col gap-4 border-b sm:flex-row sm:items-end sm:justify-between"><div><CardTitle className="text-base">Report details</CardTitle><CardDescription>Set the reporting period and project category.</CardDescription></div><div className="flex flex-col gap-1.5 sm:w-48"><label className="text-xs font-medium text-muted-foreground">Project category</label><Select defaultValue="Product Design"><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="Product Design">Product Design</SelectItem><SelectItem value="Website Redesign">Website Redesign</SelectItem><SelectItem value="Internal Operations">Internal Operations</SelectItem></SelectContent></Select></div></CardHeader><CardContent className="p-0"><div className="overflow-x-auto"><table className="w-full min-w-[860px] text-sm"><thead><tr className="border-b bg-muted/40 text-left text-xs text-muted-foreground"><th className="px-5 py-3">Task name</th><th className="px-4 py-3">Priority</th><th className="px-4 py-3">Planned vs actual</th><th className="px-4 py-3">Status</th><th className="px-4 py-3">Time spent</th><th className="px-5 py-3">Output</th></tr></thead><tbody>{tasks.map((task) => <tr key={task.name} className="border-b last:border-0"><td className="px-5 py-4 font-medium">{task.name}</td><td className="px-4 py-4"><Badge variant="outline" className={task.priority === 'High' ? 'border-accent/40 bg-accent/15 text-accent-foreground' : 'border-primary/25 bg-primary/10 text-primary'}>{task.priority}</Badge></td><td className="min-w-44 px-4 py-4"><div className="flex flex-col gap-1.5"><div className="flex justify-between text-[11px] text-muted-foreground"><span>Planned {task.planned}%</span><span>Actual {task.actual}%</span></div><Progress value={task.actual} /></div></td><td className="px-4 py-4"><Select defaultValue={task.status}><SelectTrigger className="h-8 w-32"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="Completed">Completed</SelectItem><SelectItem value="In progress">In progress</SelectItem><SelectItem value="Blocked">Blocked</SelectItem></SelectContent></Select></td><td className="whitespace-nowrap px-4 py-4 text-muted-foreground">{task.time}</td><td className="px-5 py-4 text-muted-foreground">{task.output}</td></tr>)}</tbody></table></div></CardContent></Card><div className="grid gap-4 lg:grid-cols-3"><FieldCard label="Tasks planned for next week" placeholder="Outline the key tasks you plan to focus on..." /><FieldCard label="Blockers / Challenges" placeholder="Share any blockers or support you need..." checkbox="Flag as key issue" /><FieldCard label="Achievements" placeholder="Highlight meaningful outcomes from this week..." checkbox="Flag as key achievement" /></div><div className="flex flex-col-reverse gap-3 border-t pt-5 sm:flex-row sm:justify-end"><Button variant="outline">Save draft</Button><Button onClick={onSubmitted}>{submitted ? 'Submitted' : 'Submit for review'}</Button></div></div>
}

function FieldCard({ label, placeholder, checkbox }: { label: string; placeholder: string; checkbox?: string }) { return <Card className="shadow-none"><CardHeader className="gap-1 pb-3"><CardTitle className="text-sm">{label}</CardTitle></CardHeader><CardContent className="flex flex-col gap-3"><Textarea placeholder={placeholder} className="min-h-32 resize-y" />{checkbox && <label className="flex items-center gap-2 text-xs text-muted-foreground"><input type="checkbox" className="size-4 rounded border-input accent-primary" />{checkbox}</label>}</CardContent></Card> }

function ReviewsView({ onNavigate }: { onNavigate: (view: View) => void }) { return <div className="flex flex-col gap-6"><PageHeading eyebrow="Manager workspace" title="Review queue" description="Keep submissions moving with focused feedback and clear next steps." action={<Button variant="outline" onClick={() => onNavigate('Team')}><Users data-icon="inline-start" />View team</Button>} /><div className="grid gap-3 sm:grid-cols-3"><StatCard label="Awaiting review" value="8" icon={Clock3} /><StatCard label="Needs correction" value="6" icon={AlertCircle} /><StatCard label="Approved this week" value="28" icon={CheckCircle2} /></div><Card className="shadow-none"><CardHeader><CardTitle className="text-base">Reports awaiting your review</CardTitle><CardDescription>Prioritized by submission date and status.</CardDescription></CardHeader><CardContent className="flex flex-col gap-2">{teamMembers.slice(0, 4).map((member, index) => <div key={member.name} className="flex flex-col gap-3 rounded-lg border p-4 sm:flex-row sm:items-center"><Avatar className="size-10"><AvatarFallback>{member.initials}</AvatarFallback></Avatar><div className="min-w-0 flex-1"><p className="font-medium">{member.name}</p><p className="text-sm text-muted-foreground">{index === 1 ? 'May 13 – May 19 · Product Design' : 'May 20 – May 26 · Website Redesign'}</p></div><StatusBadge status={index === 1 ? 'Needs Correction' : 'Submitted'} /><Button variant="outline" size="sm">Review report</Button></div>)}</CardContent></Card></div> }

function StatCard({ label, value, icon: Icon }: { label: string; value: string; icon: typeof Clock3 }) { return <Card className="shadow-none"><CardContent className="flex items-center justify-between p-5"><div><p className="text-sm text-muted-foreground">{label}</p><p className="mt-2 text-3xl font-semibold tracking-tight">{value}</p></div><div className="rounded-lg bg-muted p-2.5 text-muted-foreground"><Icon className="size-5" /></div></CardContent></Card> }

function TeamView() { return <div className="flex flex-col gap-6"><PageHeading eyebrow="Manager workspace" title="Team overview" description="Monitor submissions, compliance, and workload across your team." action={<Button><Plus data-icon="inline-start" />Invite member</Button>} /><div className="grid gap-3 sm:grid-cols-3"><StatCard label="Team members" value="24" icon={Users} /><StatCard label="Avg. compliance" value="94%" icon={BarChart3} /><StatCard label="Open blockers" value="9" icon={AlertCircle} /></div><Card className="shadow-none"><CardHeader><CardTitle className="text-base">Team members</CardTitle><CardDescription>Weekly submission health by member.</CardDescription></CardHeader><CardContent className="p-0"><div className="overflow-x-auto"><table className="w-full min-w-[700px] text-sm"><thead><tr className="border-y bg-muted/40 text-left text-xs text-muted-foreground"><th className="px-6 py-3">Member</th><th className="px-4 py-3">Reports</th><th className="px-4 py-3">Compliance</th><th className="px-4 py-3">Status</th><th className="px-6 py-3 text-right">Action</th></tr></thead><tbody>{teamMembers.map((member) => <tr key={member.name} className="border-b last:border-0"><td className="px-6 py-4"><div className="flex items-center gap-3"><Avatar className="size-9"><AvatarFallback>{member.initials}</AvatarFallback></Avatar><div><p className="font-medium">{member.name}</p><p className="text-xs text-muted-foreground">{member.role}</p></div></div></td><td className="px-4 py-4 text-muted-foreground">{member.reports}</td><td className="px-4 py-4"><div className="flex items-center gap-3"><Progress value={member.compliance} className="w-24" /><span className="text-xs text-muted-foreground">{member.compliance}%</span></div></td><td className="px-4 py-4"><StatusBadge status={member.status} /></td><td className="px-6 py-4 text-right"><Button size="sm" variant="outline">View reports</Button></td></tr>)}</tbody></table></div></CardContent></Card></div> }

function ProjectsView() { return <div className="flex flex-col gap-6"><PageHeading eyebrow="Workspace" title="Projects" description="See where your team is spending time and how work is progressing." action={<Button><Plus data-icon="inline-start" />New project</Button>} /><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{projects.map((project) => <Card key={project.name} className="shadow-none"><CardHeader><div className="flex items-start justify-between"><div className={`flex size-10 items-center justify-center rounded-lg ${project.color}/15 text-foreground`}><FolderKanban className="size-5" /></div><Button variant="ghost" size="icon" aria-label={`More options for ${project.name}`}><SlidersHorizontal /></Button></div><CardTitle className="pt-2 text-base">{project.name}</CardTitle><CardDescription>{project.code} · {project.members} members</CardDescription></CardHeader><CardContent className="flex flex-col gap-3"><div className="flex justify-between text-sm"><span className="text-muted-foreground">Completion</span><span className="font-medium">{project.completion}%</span></div><Progress value={project.completion} /><p className="text-xs text-muted-foreground">{project.reports}</p></CardContent></Card>)}</div></div> }

function SettingsView() { const [saved, setSaved] = useState(false); return <div className="flex flex-col gap-6"><PageHeading eyebrow="Workspace settings" title="Settings" description="Manage workspace preferences, reporting cadence, and notifications." /><div className="grid gap-6 lg:grid-cols-[220px_1fr]"><nav className="flex gap-1 overflow-x-auto lg:flex-col"><button className="whitespace-nowrap rounded-lg bg-muted px-3 py-2 text-left text-sm font-medium">General</button><button className="whitespace-nowrap rounded-lg px-3 py-2 text-left text-sm text-muted-foreground hover:bg-muted">Notifications</button><button className="whitespace-nowrap rounded-lg px-3 py-2 text-left text-sm text-muted-foreground hover:bg-muted">Members & roles</button></nav><div className="flex flex-col gap-4"><Card className="shadow-none"><CardHeader><CardTitle className="text-base">General preferences</CardTitle><CardDescription>Set defaults for your reporting workspace.</CardDescription></CardHeader><CardContent className="flex flex-col gap-4"><div className="grid gap-4 sm:grid-cols-2"><div className="flex flex-col gap-1.5"><label className="text-sm font-medium">Workspace name</label><Input defaultValue="Pulseboard" /></div><div className="flex flex-col gap-1.5"><label className="text-sm font-medium">Week starts on</label><Select defaultValue="Monday"><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="Monday">Monday</SelectItem><SelectItem value="Sunday">Sunday</SelectItem></SelectContent></Select></div></div><div className="flex items-center justify-between rounded-lg border p-4"><div><p className="text-sm font-medium">Weekly reminders</p><p className="text-xs text-muted-foreground">Send members a reminder before the weekly deadline.</p></div><input type="checkbox" defaultChecked className="size-4 accent-primary" /></div></CardContent></Card><div className="flex justify-end"><Button onClick={() => setSaved(true)}>{saved ? <><Check data-icon="inline-start" />Saved</> : 'Save changes'}</Button></div></div></div></div> }

export default function Page() {
  const [view, setView] = useState<View>('Reports')
  const [mobileOpen, setMobileOpen] = useState(false)
  const currentTitle = view === 'Overview' ? 'Overview' : view === 'Reports' ? 'Reports' : view
  return <div className="flex min-h-screen bg-background text-foreground"><Sidebar view={view} onNavigate={setView} /><div className="flex min-w-0 flex-1 flex-col"><header className="flex h-20 items-center justify-between border-b bg-card px-4 sm:px-6 lg:px-8"><div className="flex items-center gap-3"><button className="rounded-md p-2 hover:bg-muted lg:hidden" onClick={() => setMobileOpen(!mobileOpen)} aria-label="Toggle navigation">{mobileOpen ? <X className="size-5" /> : <Menu className="size-5" />}</button><div><p className="text-xs font-medium uppercase tracking-[0.14em] text-muted-foreground">Pulseboard workspace</p><h1 className="text-xl font-semibold tracking-tight sm:text-2xl">{currentTitle}</h1></div></div><div className="flex items-center gap-2 sm:gap-4"><button className="relative rounded-md p-2 text-muted-foreground hover:bg-muted hover:text-foreground" aria-label="Notifications"><Bell className="size-5" /><span className="absolute right-1.5 top-1.5 size-1.5 rounded-full bg-destructive" /></button><Separator orientation="vertical" className="hidden h-7 sm:block" /><Avatar className="size-9"><AvatarFallback className="bg-primary text-xs text-primary-foreground">JM</AvatarFallback></Avatar></div></header>{mobileOpen && <div className="border-b bg-sidebar p-3 lg:hidden"><nav className="grid grid-cols-2 gap-1" aria-label="Mobile navigation">{(['Overview', 'Reports', 'Team', 'Projects', 'Reviews', 'Settings'] as View[]).map((item) => <button key={item} onClick={() => { setView(item); setMobileOpen(false) }} className={`flex items-center gap-2 rounded-md px-3 py-2 text-sm ${view === item ? 'bg-sidebar-accent font-medium text-sidebar-accent-foreground' : 'text-sidebar-foreground/70'}`}><span className="size-1.5 rounded-full bg-current" />{item}</button>)}</nav></div>}<main className="flex-1 px-4 py-6 sm:px-6 sm:py-8 lg:px-8"><div className="mx-auto flex max-w-[1440px] flex-col gap-6">{view === 'Overview' && <OverviewView onNavigate={setView} />}{view === 'Reports' && <ReportsView onNavigate={setView} />}{view === 'Reviews' && <ReviewsView onNavigate={setView} />}{view === 'Team' && <TeamView />}{view === 'Projects' && <ProjectsView />}{view === 'Settings' && <SettingsView />}</div></main></div></div>
}
